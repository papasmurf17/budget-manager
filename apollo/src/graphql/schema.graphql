type Transaction {
    _id: ID!,
    amount: Float,
    currencyCode: String,
    description: String,
    expenseType: String,
    invoiceDate: String,
    user: String
}

type Query {
    Transactions(limit: Int): [Transaction]
    Transaction(id: ID!): Transaction
}
